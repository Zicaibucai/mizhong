# 生产环境清理记录

按用户指示执行的生产环境清理，**每一步都先做精确 ID 与状态核对**，核对不通过即中止。
本文档只记录标识与数量，**不含任何密码、Cookie、会话令牌或其他凭据**。

---

## 2026-09-18 · 测试管理员 `e2e-temp@mizhong.test`

**性质**：本轮翻译功能验收时由 `npm run admin:create` 创建的临时管理员账号。

### 核对到的事实

| 项目 | 值 |
|---|---|
| 角色 | ADMIN |
| 创建时间 | 2026-09-18T01:51:53.252Z |
| 最后登录 | 2026-09-18T02:13:47.070Z |
| 登录来源 IP | `::ffff:127.0.0.1`（经 SSH 隧道从本机访问，全部 13 次登录均为此地址） |
| 会话数 | 13 |
| 审计日志数 | 40（LOGIN 13 次、CREATE Product 1 次、UPDATE Product 26 次） |
| 首次/末次操作 | 2026-09-18T01:52:12Z 登录 → 2026-09-18T02:14:07Z 可视化商品编辑 |

时间范围与本轮验收完全吻合，确认为测试账号。

### 外键引用检查（删除前）

| 引用 | 数量 | 删除行为 |
|---|---|---|
| `Session.userId` | 13 | 级联删除 |
| `AuditLog.userId` | 40 | 置空保留（审计记录本身不删） |
| `ProductVersion.createdById` | **0** | 无阻塞引用 |

### 处置

1. **先禁用**：`enabled = false`，同时吊销全部 13 条会话（禁用只挡新登录，已签发的 Cookie 仍然有效）。
2. **验证拒绝**：对生产站点用正确密码尝试登录，停留在 `/admin/login`，提示 `Incorrect email or password.` —— **已确认无法登录**。
3. **验证鉴权仍工作**：用错误密码登录正式管理员账号，同样被拒 —— 鉴权链路正常。
4. **删除**：确认无外键阻塞后删除。

### 删除后核对

- 该账号已不存在
- 剩余账号：`2936962147@qq.com`（ADMIN，enabled=true）—— 正式管理员未受影响
- 剩余会话：47
- 审计日志：837 条（其中归属为空 715 条；删除账号的 40 条记录保留，归属置空）

---

## 2026-09-18 · 测试商品 `product_3`

**性质**：本轮翻译与 slug 验收时创建的专用测试商品。

### 核对到的事实（删除前）

| 项目 | 值 |
|---|---|
| ID | `cmu6azoxg00040v8x5dv3zhgs` |
| slug | `product_3` |
| 发布状态 | **未发布**（published = false） |
| 创建时间 | 2026-09-18T01:52:18.676Z |
| 草稿 | 有（内含本轮翻译测试产生的 10 种语言译文） |

### 一致性核对（全部通过才继续）

- ✅ published = false
- ✅ ID 精确匹配 `cmu6azoxg00040v8x5dv3zhgs`
- ✅ slug 精确匹配 `product_3`
- ✅ 不是正式商品 `tehsugouzi` / `manli`

### 关联业务数据数量

| 关联 | 数量 |
|---|---|
| ProductTranslation | 0 |
| ProductMedia（图库） | 0 |
| ProductSpecification（规格） | 0 |
| ProductVersion（历史版本） | 0 |
| Inquiry（询价） | 模型与商品无关联关系 |

### 处置

使用**项目现有的删除机制** —— 即商品编辑器「版本」分区底部的「删除商品」按钮
（`DeleteForm` + `deleteProductAction`），全程走界面点击，没有直接执行数据库删除。

- 二次确认出现，确认语中**含商品名**（`永久删除商品「…」？…`）
- 删除后跳转 `/admin/products?deleted=1`
- 该动作同时写入审计日志并触发前台缓存失效

### 删除后核对

| 检查位置 | 结果 |
|---|---|
| 数据库 | 已不存在；商品总数 2 |
| 后台商品列表 | 不含 `product_3`，列表 2 行 |
| 后台搜索 `product_3` | 无结果 |
| 前台 `/zh|/en|/ja|/ar/products/product_3` | 全部 404 |
| 前台搜索 | 0 条结果 |
| 前台目录 | 无残留；仅 `金姐`、`S型沙发特殊钩子` |

### 未受影响

`tehsugouzi` 与 `manli` 全程未被触碰：slug、发布状态、翻译内容在清理前后一致。

---

## 历史清理（2026-09-18 早些时候，已在本轮审计中向用户说明）

| slug | ID | 状态 | 是否可从备份恢复 |
|---|---|---|---|
| `product-3` | `cmu58pgg8000fs7rrfn630zgw` | 未发布、无草稿、无翻译、无图库、无规格、无版本（**完全为空**） | 可，见 `before-locale-expansion-20260918-090141.sql` |
| `product_4` | —— | 未发布、含测试草稿 | 不可，创建时间晚于最后一份备份 |

两条均**未恢复**（用户明确指示不恢复）。

---

## 2026-09-18 · 第二轮：`cleanup-temp@mizhong.test`

执行上述清理时临时创建的第二个管理员账号，用于走界面完成删除与复制验收。
处理方式与 `e2e-temp@` 完全一致：

| 项目 | 值 |
|---|---|
| 角色 | ADMIN |
| 创建时间 | 2026-09-18T03:00:07.683Z |
| 最后登录 | 2026-09-18T03:18:55.528Z |
| 会话 / 审计 | 12 / 21 |
| ProductVersion 外键引用 | 0 |

1. 先禁用并吊销 12 条会话；
2. 对生产站点用正确密码尝试登录 —— 停留在 `/admin/login`，**确认无法登录**；
3. 确认无外键阻塞后删除；
4. 删除后剩余账号：`2936962147@qq.com`（ADMIN，enabled=true）。

---

## 待确认删除的测试商品（**尚未删除**）

本轮验收新产生的测试商品，全部未发布。按指示先列出，等待确认后再删。

| slug | ID | 状态 | 说明 |
|---|---|---|---|
| `product_3` | `cmu6dz8uq00040vcvdy8ds5v8` | 未发布 | 「零调用」翻译优化验收用，草稿内含 10 种语言译文 |
| `product_3_copy` | `cmu6e3b5n00050vi6jj9v521j` | 未发布 | 复制功能验收产物 |
| `product_3_copy_2` | `cmu6e3lxh00090vi6crs1kf7c` | 未发布 | 重复复制时 slug 递增的验收产物 |

正式商品 `tehsugouzi`、`manli` 全程未被触碰。
